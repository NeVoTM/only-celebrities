version: "1"
services:
  - type: web
    name: only-celebrities
    runtime: static
    region: oregon
    plan: free
    buildCommand: "true"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
